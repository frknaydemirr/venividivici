import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-cities',
  imports: [RouterLink],
  templateUrl: './cities.html',
  styleUrl: './cities.css',
})
export class Cities {

}
